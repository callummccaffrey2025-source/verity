/**
 * Temporary shim to unblock typecheck.
 * Your new pages already use types-compat; re-export it here so
 * any existing imports from "@/lib/types" still work.
 */
export * from './types-compat';
