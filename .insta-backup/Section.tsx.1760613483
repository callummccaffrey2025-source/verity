export default function Section({
  id, title, caption, children
}: { id: string; title: string; caption?: string; children: React.ReactNode }) {
  return (
    <section id={id} className="scroll-mt-24">
      <div className="card p-4 md:p-5">
        <header className="flex items-end justify-between pb-3 border-b border-white/10">
          <h2 className="section-title text-lg md:text-xl text-white">{title}</h2>
          {caption ? <span className="text-xs text-white/60">{caption}</span> : null}
        </header>
        <div className="pt-4">
          {children}
        </div>
      </div>
    </section>
  );
}
