export default function ValueProps() {
  // Keep this non-invasive: render nothing for now to avoid duplicating UI
  // (your page already has the value prop cards). Swap this out later if desired.
  return null;
}
