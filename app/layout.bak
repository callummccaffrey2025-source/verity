import "./globals.css";
import type { Metadata, Viewport } from "next";

export const metadata = {
  title: "Verity — Transparent politics, made simple.",
  description: "Live bill stages, MP voting history, and plain-English summaries. Built for Australians.",
};

import SiteHeader from "../components/SiteHeader";export const viewport: Viewport = {
  themeColor: [
    { media: "(prefers-color-scheme: light)", color: "#ffffff" },
    { media: "(prefers-color-scheme: dark)",  color: "#0b0b0b" },
  ],
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className="bg-black text-zinc-200">
        <SiteHeader />
        {children}
      </body>
    </html>
  );
}
