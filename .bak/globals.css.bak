@tailwind base;
@tailwind components;
@tailwind utilities;

/* Minimal base to match the preview look */
:root { color-scheme: dark; }
html, body { height: 100%; background-color: #0a0a0a; } /* zinc-950 */
* { outline-offset: 2px; }
