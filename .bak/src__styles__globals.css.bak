@tailwind base;
@tailwind components;
@tailwind utilities;

:root { --bg: #0b0f14; --fg: #e5e7eb; --accent: #10b981; }
html, body { height: 100%; }
body { background: var(--bg); color: var(--fg); }
a { color: var(--accent); }
